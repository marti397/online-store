<div class="container">
    <div class="row">
        <div class="col text-center mainFont font-weight-normal text-uppercase mt-sm-3">
            {{title}}
        </div>
    </div>
    <div class="row">
        <div id="success" class="col text-center mainFont font-weight-normal text-uppercase mt-sm-3 {{#if noMessage}}hidden{{/if}}">
            {{messages}}
        </div>
    </div>
    <div class="row mainFont mt-sm-3">
        <div class="col">
            FILTROS
        </div>
    </div>
    <!--bueno-->
    <div class="row">
        {{#each products}}
        <div class="col-sm-3 col-6 mt-sm-5 mt-3 productImg">
            <!--<img class="img-fluid" src="{{ imagePath }}" alt="product">-->
            <div id="carouselExampleControls{{this._id}}" class="carousel slide" data-ride="carousel">
                <div class="carousel-inner">
                    <div class="carousel-item active">
                    <img class="d-block w-100" src="{{ imagePath }}" alt="First slide">
                    </div>
                    <div class="carousel-item">
                    <img class="d-block w-100" src="{{ imagePath }}" alt="Second slide">
                    </div>
                    <div class="carousel-item">
                    <img class="d-block w-100" src="{{ imagePath }}" alt="Third slide">
                    </div>
                </div>
                <a class="carousel-control-prev" href="#carouselExampleControls{{this._id}}" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon productArrowPrev" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#carouselExampleControls{{this._id}}" role="button" data-slide="next">
                    <span class="carousel-control-next-icon productArrowNext" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>
            <!--End of Carousel-->
            <div class="secondFont">{{title}}</div>
            <div class="secondFont">${{price}} MXN</div>
            
            <!-- Button trigger modal -->
            <button type="button" class="btn modalBtn position-absolute fixed-top ml-2" data-toggle="modal" data-target="#exampleModalCenter{{this._id}}">
                <img src="/images/search.svg" alt="search" width="20" height="20" >
            </button>
            <!-- Modal -->
            <div class="modal fade" id="exampleModalCenter{{this._id}}" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
                <div class="modal-content">
                <div class="modal-body">
                    <div class="container-fluid">
                        <div class="row">
                            <div class="col">
                                <img class="img-fluid" src="{{ imagePath }}" alt="product">
                            </div>
                            <div class="col">
                                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                                </button>
                                <div class="mainFont mt-3 font-weight-normal">{{title}}</div>
                                <div class="mainFont mt-1">{{descr}}</div>
                                <div class="mainFont mt-1 font-weight-normal">${{price}} MXN</div>
                                <div class="mainFont mt-4">Envios y devoluciones gratis</div>
                                <div class="mainFont">En ordenes de mas de $2,000 MX</div>
                                <div class="mainFont mt-4">Descripción</div>
                                <div class="mainFont">{{details}}</div>
                                <a href="/add-to-cart/{{this._id}}" class="btn btn-outline-dark position-absolute fixed-bottom mainFont rounded-0">Agregar</a>
                            </div>
                        </div>
                    </div>
                </div>
                </div>
            </div>
            </div>
            <!-- End of Modal -->
        </div>
        {{/each}}
    </div>
</div>